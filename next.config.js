/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* i18n is handled manually through App Router params or Context for extreme flexibility */
};

module.exports = nextConfig;
